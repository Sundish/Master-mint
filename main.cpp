#include "graphfx.hpp"


int main (void)
{
     // std::vector<int> t{4, 4};
     // std::cout << t.front() << t.back() << "\n";
     // t.emplace_back(5);
     // std::cout << t.front() << t.back() << "\n";
     print_full_table({"aefd", "bcfe", "abde"}, {"1111", "1212", "2200"}, 10);
     return 0;
}
